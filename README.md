# Transaction-Milestone-Tracker
add transactions with address + agent, click through 13 stages to advance the file, track a COE target date with a countdown, and add notes per file. Progress bar shows how far along each deal is. Data saves in localStorage so it persists between sessions.
